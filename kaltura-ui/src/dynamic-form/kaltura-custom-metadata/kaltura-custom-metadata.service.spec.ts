import { getTestBed, async, inject, TestBed } from '@angular/core/testing';

describe('extract controls from metadata parser', () =>
{
    it('represent text single value correctly',async(inject([],()=> {
        expect(true).toBe(true);
    })));
});

