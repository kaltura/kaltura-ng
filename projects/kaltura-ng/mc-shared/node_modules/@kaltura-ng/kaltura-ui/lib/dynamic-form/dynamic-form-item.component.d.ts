import { TemplateRef, QueryList, OnInit } from '@angular/core';
import { FormGroup, FormBuilder, AbstractControl } from '@angular/forms';
import { DynamicSectionControl } from './controls/dynamic-section-control';
import { DynamicFormService } from './dynamic-form.service';
import { DynamicFormControlBase } from './dynamic-form-control-base';
export declare class DynamicFormItem implements OnInit {
    private _formBuilder;
    private _dynamicFormService;
    control: DynamicFormControlBase<any>;
    form: FormGroup;
    isRequired: boolean;
    _templates: QueryList<TemplateRef<any>>;
    labelTemplate: TemplateRef<any>;
    constructor(_formBuilder: FormBuilder, _dynamicFormService: DynamicFormService);
    asDynamicSection(control: DynamicFormControlBase<any>): DynamicSectionControl;
    asFormGroup(control: AbstractControl): FormGroup;
    ngOnInit(): void;
    initItem(dynamicControl: DynamicFormControlBase<any>): FormGroup;
    addItem(dynamicControl: DynamicFormControlBase<any>): void;
    removeItem(dynamicControl: DynamicFormControlBase<any>, i: number): void;
}
