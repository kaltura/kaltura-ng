import { ElementRef, OnDestroy } from '@angular/core';
export declare type TooltipPositions = 'top' | 'left' | 'right' | 'bottom';
export declare class KTooltipDirective implements OnDestroy {
    private elementRef;
    constructor(elementRef: ElementRef);
    private _tooltipElement;
    private _tooltipContent;
    private _shouldBeVisible;
    private _tooltipHeight;
    private _tooltipWidth;
    tooltipResolver: string | ((val: any) => string);
    escape: boolean;
    tooltipOffset: number;
    placement: TooltipPositions;
    delay: number;
    maxWidth: number;
    followTarget: boolean;
    showOnEllipsis: boolean;
    set kTooltip(value: any);
    private isValidContent;
    private _updateTooltipElement;
    onMouseMove(): void;
    onMouseEnter(): void;
    onMouseLeave(): void;
    private isInEllipsis;
    private _removeTooltipElement;
    private _isShowingTooltip;
    private _updateTooltipElementContent;
    private _updateTooltipElementPosition;
    ngOnDestroy(): void;
}
