import { OnInit, ElementRef, Renderer2 } from '@angular/core';
import { AreaBlockerMessage } from './area-blocker-message';
export declare class AreaBlockerComponent implements OnInit {
    private _renderer;
    private _showLoader;
    _message: AreaBlockerMessage;
    private _showLoaderHandler;
    areaBlockerContainer: ElementRef;
    spinnerContainer: ElementRef;
    bodyScroll: boolean;
    spinnerMarginTop: number;
    classes: string;
    set showLoader(value: boolean);
    get showLoader(): boolean;
    set message(value: AreaBlockerMessage | string);
    constructor(_renderer: Renderer2);
    handleAction(button: {
        action: () => void;
    }): void;
    ngOnInit(): void;
}
