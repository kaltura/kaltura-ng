import { ElementRef, AfterViewInit, OnDestroy, Renderer2 } from '@angular/core';
import { StickyScrollService } from '../services/sticky-scroll.service';
import { StickyDirective } from '../directives/sticky.directive';
export declare class StickyComponent implements AfterViewInit, OnDestroy {
    private _stickyScrollService;
    private _renderer;
    wrapperHeight: number;
    scrollOffset: number;
    stickyClass: string;
    container: any;
    stickyId: string;
    sticksTo: string;
    content: ElementRef;
    stickyDiv: ElementRef;
    _sticky: StickyDirective;
    constructor(_stickyScrollService: StickyScrollService, _renderer: Renderer2);
    ngAfterViewInit(): void;
    private _updateLayout;
    updateLayout(manual?: boolean): void;
    _updateDimensions(): void;
    ngOnDestroy(): void;
}
