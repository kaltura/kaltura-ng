import { OnDestroy } from '@angular/core';
import { Observable } from 'rxjs/Observable';
export declare class StickyScrollService implements OnDestroy {
    private _scrollSubject;
    scrollStatus$: Observable<{}>;
    private _resizeSubject;
    resizeStatus$: Observable<{}>;
    private _layoutSubject;
    layoutSubject$: Observable<{}>;
    private stickyElements;
    private manageScrollFunc;
    private manageResizeFunc;
    constructor();
    ngOnDestroy(): void;
    manageScrollEvent(): void;
    manageResizeEvent(): void;
    attach(id: string): void;
    detach(id: string): void;
    update(id: string, height: number, offset: number): void;
}
