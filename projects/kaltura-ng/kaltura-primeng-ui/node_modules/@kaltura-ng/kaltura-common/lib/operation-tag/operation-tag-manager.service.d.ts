import { Observable } from 'rxjs/Observable';
import { OperationTagManagerProxy } from './operation-tag-store-mediator';
export declare class OperationTagManagerService implements OperationTagManagerProxy {
    private _tagStatus;
    tagStatus$: Observable<{
        [key: string]: number;
    }>;
    constructor();
    increase(tag: string): void;
    decrease(tag: string): void;
}
